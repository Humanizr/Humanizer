{
  "Globs": [
    "**/.git/**",
    "**/bin/**",
    "**/obj/**",
    "**/nuget.config",       // exact file at repo root
    ".flow/**"               // task metadata (commit SHAs are not credentials)
  ]
}
